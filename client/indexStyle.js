import { styled } from '@mui/material/styles';

const appStyle = {
	height: '100%',
	width: '100%',
	margin: '0',
	position: 'fixed',
};

const navStyle = {
	position: 'relative',
	height: '70px',
};

const contentStyle = {
	// background-color:'red'
	display: 'flex',
	position: 'relative',
	border: '1px solid red',
	gap: '10px',
	height: '100%',
};

const infoStyle = {
	border: 'inherit',
	flex: '1',
};

const reviewStyle = {
	border: 'inherit',
	flex: '3',
	overflow: 'auto',
};
export default styled(() => ({}));
